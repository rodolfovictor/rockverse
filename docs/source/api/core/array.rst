.. _Array class:

rockverse.core.Array
====================

.. currentmodule:: rockverse.core

.. autoclass:: Array

Attributes
----------

.. autosummary::
    :toctree: _autogen

    ~Array.ndim
    ~Array.shape
    ~Array.data
    ~Array.values
    ~Array.name
    ~Array.unit
    ~Array.description
    ~Array.latex_name
    ~Array.latex_unit
    ~Array.dim_names
    ~Array.dim_units
    ~Array.dim_descriptions
    ~Array.dim_latex_names
    ~Array.dim_latex_units
    ~Array.zgroup

Methods
-------

.. autosummary::
    :toctree: _autogen

    ~Array.validate
    ~Array.get_name
    ~Array.set_name
    ~Array.get_unit
    ~Array.set_unit
    ~Array.get_description
    ~Array.set_description
    ~Array.get_latex_name
    ~Array.set_latex_name
    ~Array.get_latex_unit
    ~Array.set_latex_unit
    ~Array.h5_dump
